Create ONE Android launcher icon preview using the supplied image as the edit target: the original Qinglong panel logo.
Preserve its distinctive green Chinese dragon silhouette, pose, head, curling tail, central green disk, enclosing circular green ring and internal negative spaces as faithfully as possible. Do not invent a different dragon or simplify it into an unrelated symbol.
Place the complete emblem centered on a solid pure white square canvas. The green enclosing ring should have an outer diameter of 68 percent of the canvas width, leaving 16 percent even white padding on all four sides. Nothing is cropped. Keep the original saturated emerald green and sharpen the raster edges into clean flat graphic contours.
Output only the single square icon artwork, 1024 by 1024 pixels. No text, lettering, badges, gradients, shadows, 3D, texture, rounded-square border, presentation layout or phone mockup. The white background fills the square edge to edge. This is a faithful adaptation of the reference logo with better launcher-icon spacing, not a new brand design.
