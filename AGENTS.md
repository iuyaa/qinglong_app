# 项目范围

开发连接青龙面板的 Android App。根目录的 `青龙客户端_2.7.2.apk` 是旧版参考，保留原件。

## 用户授权的真实面板测试边界

- 使用用户指定的本机 9333 调试端口浏览器及其中的青龙面板标签页。
- 用户现有任务和脚本只读：禁止编辑、执行、删除、启停、改定时、排序或其他修改。
- 允许新建专用测试任务，并仅操作本次创建、已记录准确 ID 的测试任务；名称使用 `codex_android_test_` 前缀。前缀本身不构成所有权证明。
- 测试命令仅输出测试标记，不调用现有脚本，不访问或修改业务数据。避免自动定时触发，完成后停用或删除自己创建的测试任务并记录结果。
- 禁止“运行全部”、全选执行或任何可能包含现有任务的批量操作。
- 环境变量、订阅、依赖、系统设置及其他现有资源默认只读；没有额外授权不修改。
- 不把密码、Token、Client Secret 或业务环境变量内容写入代码、日志、截图或报告。
- 页面查看、静态分析、实际接口测试和 Android 真机测试应分别报告，未执行的验证标为 `NOT_RUN`。
